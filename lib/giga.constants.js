var constants = constants || (function() {

    const toastTypes = {
        WARNING: 'warning',
        FAILURE: 'failure',
        SUCCESS: 'success'
    }

    return {
        TOAST: toastTypes
    }
})();